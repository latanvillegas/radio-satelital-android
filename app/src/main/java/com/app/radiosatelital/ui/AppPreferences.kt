package com.app.radiosatelital.ui

enum class RadioLayoutMode(val label: String) {
    OneRow("1 fila"),
    TwoRows("2 filas"),
}

enum class RadioCardSizeMode(val label: String) {
    Compact("Compacto"),
    Normal("Normal"),
    Large("Grande"),
}
